<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HexEditor.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HexEditor.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="HexEditor.Windows">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="MainGrid" Loaded="MainGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="titleBar" Grid.Row="0" Title="Hex.NET" />

        <MenuBar Grid.Row="1">
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="New" AccessKey="N" />
                <MenuFlyoutItem Text="Open...">
                    <MenuFlyoutItem.Command>
                        <XamlUICommand x:Name="OpenCommand" AccessKey="O" ExecuteRequested="OpenCommand_ExecuteRequested" />
                    </MenuFlyoutItem.Command>
                </MenuFlyoutItem>
                <MenuFlyoutItem Text="Save" AccessKey="S" />
            </MenuBarItem>
        </MenuBar>

        
    </Grid>
</Window>
